# Deferred work

The following items are intentionally outside the current implementation:

- resumable dataset generation and stable sample identities
- a single hierarchical dataset format with a generation manifest
- solver retry and repair policies
- TE-safe relative-error definitions
- configurable Knox and other domain-specific validation metrics
- auditing scientific labels and multipole indexing in emulator-zoo validators
- atomic complete-artifact publication
- additional neural-network training backends
